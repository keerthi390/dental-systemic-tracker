services:
  - type: web
    name: dental-systemic-tracker
    runtime: python311
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0
